def test_has_checksum():
    assert False, "does not contain an object checksum"


def test_has_reference_path():
    assert False, "does not contain a reference path"


def test_has_name():
    assert False, "does not contain a name"


def test_has_node_type():
    assert False, "does not contain a node type identifier"
